{
  "name": "@otto/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit && echo \"worker runs from source via tsx\"",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@otto/core": "workspace:*",
    "@otto/db": "workspace:*",
    "@otto/shared": "workspace:*",
    "bullmq": "^6.3.4",
    "ioredis": "^6.0.0"
  }
}
